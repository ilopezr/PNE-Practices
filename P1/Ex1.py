from Seq1 import Seq

""" Write a python program that creates an object with the sequence "ACTGA" and prints 
its length and the sequence itself."""

print('--------- |Exercise 1| ---------')
seq = Seq('ACTGA')

print('Sequence' + str(1) + ': (Length: ' + str(seq.len()) + ') ' + str(seq))
